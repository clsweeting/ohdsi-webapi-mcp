# Example environment configuration
# Copy this file to .env and update with your actual values

# Required: Base URL for your OHDSI WebAPI instance
WEBAPI_BASE_URL=https://atlas-demo.ohdsi.org/WebAPI

# Optional: Default CDM source key for cohort operations (e.g., "SYNPUF1K", "EUNOMIA")
# Only needed for tools like estimate_cohort_size - can be provided per-tool instead
# WEBAPI_SOURCE_KEY=EUNOMIA

# Optional: API key if your WebAPI requires authentication
# WEBAPI_API_KEY=your-api-key-here

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# HTTP Server Configuration (for ohdsi-webapi-mcp-http)
# Port to run the HTTP server on
MCP_PORT=8000

# Host to bind the HTTP server to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
MCP_HOST=0.0.0.0
